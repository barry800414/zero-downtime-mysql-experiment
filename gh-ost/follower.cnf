[mysqld]
server-id = 2

# name of bin log
log-bin=follower-bin

log_slave_updates=ON

# relay_log = relay_bin
# relay-log-index = relay-bin.index
# read-only=1 # 除非有SUPER權限，否則只讀
# super-read-only=1 # SUPER權限也是只讀
